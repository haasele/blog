---
/**
 * TOC - sidebar TOC component (backward-compatible wrapper)
 *
 * @deprecated Use import { SidebarTOC } from "@components/features/toc"
 *
 * This wrapper will be removed in the next major version.
 * Still referenced by SideBar.astro
 */

import type { MarkdownHeading } from "astro";

import SidebarTOC from "../../features/toc/SidebarTOC.astro";

interface Props {
	class?: string;
	headings?: MarkdownHeading[];
}

const { class: className, headings } = Astro.props;
---

<SidebarTOC class={className} headings={headings} />
